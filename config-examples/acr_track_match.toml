# acr_track_match defaults
# CLI flags always override values from this file.

refs = ["reference_tracks"]
live = true
rtss = true
rtss_owner = "acr_track_match"
rtss_slot = 3

sectors_shp = "timing/sectors.shp"
sector_track_field = "src_layer"
sector_id_field = "seg_id"
timing_db = "timing/timing.db"

track_lock_after_sec = 10.0
track_unlock_speed_kmh = 3.0
track_unlock_hold_sec = 5.0
## Persist detected staged starts as GeoJSON points (used for future preselection too)
start_points_geojson = "timing/start_points.geojson"
start_prefilter_radius = 20.0
beep_on_split = true

# Split feedback audio (rodio → default playback device). Use mode = "wav" + paths for custom sounds.
[beep]
mode = "sine"
faster_freq_hz = 1700.0
faster_duration_ms = 45
slower_freq_hz = 950.0
slower_duration_ms = 140
gap_ms = 200
volume = 0.5
# faster_wav = "sounds/split_faster.wav"
# slower_wav = "sounds/split_slower.wav"

# Pre-recorded pacenotes (trigger on marker crossing while track lock is active).
[pacenotes]
enabled = false
pacenotes_dir = "timing/pacenotes"
# stage = "sisteron_mezien"
# geojson = "timing/pacenotes/sisteron_mezien.geojson"
voice_dir = "voices/elevenlabs_en"
lead_sec = 1.0
slow_corner_extra_lead_sec = 1.0
protected_corner_gear = 2
critical_start_reserve_sec = 1.2
skippable_corner_gear = 4
gear_advance_hz = 5
# Minimum ACC gear (1=first, 7=sixth) before gear-based lead is applied.
gear_advance_gear = 3
gear_reference_severity = 6
gear_step_ms = 300
skip_buffer_m = 5.0
trigger_radius_m = 12.0
min_speed_kmh = 8.0
volume = 0.7

# Optional tuning (defaults shown)
# downsample = 10
# buffer = 30.0
# required_ratio = 0.5
# history_points = 200
# rate = 5
# min_ref_spacing = 2.0
# sector_cooldown_ms = 500
# sector_radius = 25.0
# track_keep_max_dist = 15.0
# track_switch_min_gain = 0.8
# rtss_clear_all = false
