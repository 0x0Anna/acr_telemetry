[package]
name = "acr_timing_protocol"
version = "0.1.0"
edition = "2024"
description = "Timing event schema and transport envelopes (engine → presenter / store)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
